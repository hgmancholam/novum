Act as the EvalEngineer agent. Read the memory bank + most recent iteration history + failure taxonomy. Verify hypothesis pre-registration, confirm telemetry exists, run eval against the golden set, build the 4-axis scorecard, and produce a PASS/FAIL/INCONCLUSIVE verdict for:

$ARGUMENTS
