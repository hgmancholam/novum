# Code Review — BRD-04 User Identity (Iteration 1)

**Review ID:** REVIEW-BRD-04-1
**BRD:** [BRD-04-user-identity.md](../brds/BRD-04-user-identity.md)
**Plan:** [IP-04-user-identity.md](../implementation-plans/IP-04-user-identity.md)
**Date:** 2026-05-26
**Reviewer:** Reviewer Agent
**Iteration:** 1 / 5

---

## Score: **9.5 / 10** — APPROVED

## Test results (verified)
- Backend: **146 passed / 0 failed** (38 new auth tests).
- Frontend: **113 passed / 0 failed** (18 new auth tests).
- `ruff check`, `pyright --strict`, `tsc --noEmit` clean.

## Strengths
- **AC coverage complete** (AC-01..AC-05), all mapped per IP-04 §4.
- **Security tight:** SHA-256 hash only, `secrets.compare_digest`, symmetric `InvalidTokenError` for unknown-user vs wrong-token (no info leak), `/api/auth/users/{username}` returns only `username` + `created_at` (token/token_hash absent, tested).
- **Architectural rules honored:** storage not-seam (direct `AsyncSession`), single source for FastAPI deps, no Redis/Docker/LangChain leakage.
- **Strict typing clean** (backend + frontend); L-006 `exactOptionalPropertyTypes` honored in `userStore` and `UsernameModal`.
- **All IP-04 §5 tightenings honored** (§5.3 `/verify` never raises; §5.4 `/api/auth/users/{username}` prefix kept; §5.5 symmetric error; §5.7 `isVerifying: true` initial; §5.8 `vi.spyOn(fetch)` not MSW).
- **Conftest follows L-005** with SQLite dialect hooks; new `seeded_user` / `auth_headers` fixtures cleanly upgrade BRD-03 route tests.
- **Modal a11y above spec:** `role="dialog"`, `aria-modal`, `aria-labelledby`.

## Blockers
None.

## Nits (non-blocking)
1. No dedicated `UsernameModal.test.tsx`; modal is exercised indirectly via `userStore` tests. BRD-04 §7 lists it as a 100% Vitest target — small follow-up.
2. BRD-04 §4.2 documents `/api/users/{username}`; implementation correctly uses `/api/auth/users/{username}` per IP-04 §5.4. Doc-side patch only.
3. `UserResponse.created_at` is `str` via manual `.isoformat()`; Pydantic v2 could serialize `datetime` natively. Cosmetic.

## Verdict
**APPROVE.** Implementation meets the 9/10 quality gate on the first iteration. Ready for completion (F5).
