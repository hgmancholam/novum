# Codemap

> Auto-generated by `scripts/gen_codemap.py`. Do NOT edit manually.
> Regenerate after significant refactors: `python scripts/gen_codemap.py`

---

## Backend (`backend/app/`)

### `agent/`

**`backend/app/agent/complexity.py`**
  def derive_complexity_hint()  # Derive complexity hint using deterministic heuristic (BRD-22 §4.5).

**`backend/app/agent/domain_allowlists.py`**
  def allowlist_for()  # Return the curated whitelist for ``domain`` (empty tuple if absent).

**`backend/app/agent/dynamic_allowlist.py`**
  class DynamicAllowlistOutput  # Structured output for the dynamic allowlist classifier prompt.
  _PROMPT_TEMPLATE  # const
  async def propose_allowlist()  # Ask the classifier LLM for 3-8 authoritative domains for ``question``.

**`backend/app/agent/experts/taxonomy.py`**
  def match()  # Return credibility multiplier for a source based on expected expert match.

**`backend/app/agent/instant_cache.py`**
  def normalise_question()  # Normalise question string for cache key matching.
  class CachedRun  # Payload stored in instant cache for a high-confidence terminal run.
  def record_run()  # Record a terminal high-confidence run in the instant cache.
  def try_replay()  # Attempt to retrieve a cached answer for the given (user, question).
  def reset_instant_cache()  # Clear all cached runs. Used by tests and shutdown hooks.

**`backend/app/agent/lane_router.py`**
  def select_lane()  # Deterministic lane routing for IP-25.
  def apply_lane_budgets()  # Populate per-run global hard caps on ``state`` from settings.

**`backend/app/agent/lanes/deep.py`**
  async def execute_deep_lane()  # Execute DEEP lane pipeline: hypotheses → ReAct → synth → CoVe → judge.

**`backend/app/agent/lanes/fast.py`**
  _FAST_RESULTS_PER_SOURCE  # const
  _FAST_S_THRESHOLD_DEFAULT  # const
  _FAST_S_THRESHOLD_TRIVIAL_STATIC  # const
  async def execute_fast_lane()  # Execute FAST lane pipeline: search → synth → mini-judge.

**`backend/app/agent/meta_judge_hook.py`**
  class _SyntheticJudgeSignal  # Duck-typed stand-in used when no judge ruling exists yet.
  async def maybe_run_meta_judge()  # Run VoC + Adversarial Completeness (BRD-26 §4.6).

**`backend/app/agent/orchestrator.py`**
  class AgentOrchestrator  # Drives a ``RunState`` through the research FSM.

**`backend/app/agent/question_index.py`**
  class PriorRunHint  # What the planner is allowed to see from past runs (WP-6 G6).
  class QuestionEmbeddingIndex  # LRU index of prior run questions with embeddings.
  def get_index()  # Get the global question index singleton.
  def reset_index()  # Reset the global index (for tests and shutdown).

**`backend/app/agent/react/actions.py`**
  class SearchAction  # Search action: query the Source seam.
  class DeepFetchAction  # Deep fetch action: retrieve full content from a URL.
  class EvaluateHypothesisAction  # Evaluate hypothesis action: mark hypothesis as confirmed or refuted.
  class FinishAction  # Finish action: terminate the ReAct loop with a reason.

**`backend/app/agent/react/history.py`**
  _TOKENIZER  # const
  class ReactStep  # Single step in the ReAct loop history.
  class HistorySummary  # LLM response for history summarization.
  async def summarize_history_if_needed()  # Summarize history if token count exceeds threshold.

**`backend/app/agent/react/loop.py`**
  _MAX_RETRIES_PER_STEP  # const
  _MAX_RESULTS_PER_SEARCH  # const
  class ThoughtOutput  # LLM structured output for thought generation.
  class _ReactJudgeSignal  # Duck-typed `judge_signal` for `maybe_run_meta_judge` (BRD-26 §4.13).
  async def run_react_loop()  # Execute ReAct loop: Thought → Action → Observation → Check stopping.

**`backend/app/agent/react/prompts.py`**
  REACT_THOUGHT_PROMPT  # const
  REACT_ACTION_PROMPT  # const
  REACT_HISTORY_SUMMARIZATION_PROMPT  # const

**`backend/app/agent/run_state.py`**
  class EvidenceItem  # In-memory evidence linked to an emitted ``EvidenceAddedEvent``.
  class RunState  # Ephemeral mutable state for one agent run.

**`backend/app/agent/runner.py`**
  class AgentRunner  # Owns the lifecycle of orchestrator tasks for in-flight runs.

**`backend/app/agent/source_hints.py`**
  def build_source_hints()

**`backend/app/agent/sources_authority/tiers.py`**
  def match()  # Return the :class:`AuthorityTier` for *source_url_or_host*.

**`backend/app/agent/states.py`**
  class AgentState  # States of the research agent FSM.
  def can_transition()  # Return True if ``from_state -> to_state`` is a legal transition.
  def is_terminal()  # Return True if the state is terminal (no further transitions allowed).

**`backend/app/agent/tasks/analyze.py`**
  COVERAGE_MIN_EVIDENCE  # const
  COVERAGE_MIN_AVG_CONFIDENCE  # const
  UNCOVERABLE_AFTER_ROUNDS  # const
  async def analyze_evidence()  # Mark pending claims as covered or uncoverable; detect contradictions.

**`backend/app/agent/tasks/classify.py`**
  _EMPTY_COMPARATIVE_MARKERS  # const
  _RECENT_YEAR_THRESHOLD  # const
  def derive_temporal_sensitivity()  # Deterministic heuristic per BRD-23 §4.5 (WP-1). Pure, no LLM call.
  class AmbiguityDimensions  # LLM structured output for ambiguous comparative questions (G9).
  async def classify_question()  # Classify ``question`` and derive complexity hint (BRD-22).
  async def detect_empty_comparative()  # Detect underspecified comparative questions and emit AmbiguityDetectedEvent.
  async def classify_dimensions()  # Call the classifier LLM to enumerate evaluation dimensions for ambiguous questio

**`backend/app/agent/tasks/cove.py`**
  class CoveQuestions  # Structured output for verification questions generation.
  class CoveVerdict  # Structured output for verification verdict.
  async def generate_verification_questions()  # Generate 3 verification questions for a draft answer.
  async def verify_question()  # Verify a question against fresh evidence.

**`backend/app/agent/tasks/deep_fetch.py`**
  async def maybe_deep_fetch()  # Run the WP-2 deep-fetch escalation pass.

**`backend/app/agent/tasks/draft.py`**
  class IssueToClaimMapping  # Inline structured output for the disconfirmation mapping step.
  class _RawSynthesizerPayload
  _LIST_OF_STR_FIELDS  # const
  async def draft_answer()  # Synthesize the final answer using all collected evidence.
  async def evaluate_with_judge()  # Call the judge and assemble a ``JudgeRuledEvent`` (WP-5 extensions).
  async def map_issues_to_claims()  # Map judge issues to the claim IDs that should be re-opened (RF-15).
  async def draft_best_effort_fallback()  # C3: regenerate the draft as a BEST_EFFORT answer when the judge cap fires.

**`backend/app/agent/tasks/hypotheses.py`**
  class HypothesisDraft  # Draft hypothesis from LLM (no UUID yet).
  class HypothesesList  # Container for LLM-generated hypotheses.
  async def generate_hypotheses()  # Generate 2-4 competing hypotheses for the question.

**`backend/app/agent/tasks/normalize.py`**
  NORMALIZER_SYSTEM_PROMPT  # const
  async def normalize_question()  # Normalize ``question`` and return the corresponding event.

**`backend/app/agent/tasks/plan.py`**
  async def create_plan()  # Create the initial plan for ``question`` (WP-6 + BRD-22).
  async def critique_plan()  # Self-critique the current plan.
  async def revise_plan()  # Revise the plan after a rejected critique (BRD-22).

**`backend/app/agent/tasks/replan.py`**
  class PlanGapsOutput  # Structured response from the planner LLM for gap detection.
  async def identify_plan_gaps()  # Call planner LLM to identify uncovered angles or sub-questions.

**`backend/app/agent/tasks/search.py`**
  _MAX_CLAIMS_PER_ROUND  # const
  _RESULTS_PER_SEARCH  # const
  _MIN_RELEVANCE_THRESHOLD  # const
  async def execute_search_round()  # Issue one cascading search per pending claim in parallel.

**`backend/app/agent/tasks/select_answer_kind.py`**
  class AnswerKindInputs  # Inputs to :func:`select_answer_kind`. All scores in ``[0, 1]``.
  _DIRECT_MIN_S  # const
  _DIRECT_MIN_COVERAGE  # const
  _DIRECT_MIN_AGREEMENT  # const
  _WEIGHTED_AGREEMENT_CEILING  # const
  _BEST_EFFORT_COVERAGE_FLOOR  # const
  _ZERO_EVIDENCE_COVERAGE_FLOOR  # const
  def select_answer_kind()  # Return the ``AnswerKind`` for the given run state (deterministic).

### `auth/`

**`backend/app/auth/token.py`**
  def generate_token()  # Return a 64-char hex token (32 random bytes).
  def hash_token()  # Return the SHA-256 hex digest of `token`.
  def verify_token()  # Constant-time verify `token` against a stored SHA-256 hash.

### `confidence/`

**`backend/app/confidence/calculator.py`**
  class ConfidenceCalculator  # Compute structural + final confidence and check sufficiency.

**`backend/app/confidence/kind_ceiling.py`**
  def is_stale_majority()  # BRD-23 WP-1: >= 50% of evidence rows have a stale ``source_published_date``.
  def apply_ceiling()  # Apply the kind-specific ceiling to the raw structural score.

**`backend/app/confidence/mismatch.py`**
  _STRUCTURAL_HIGHER_FLAG  # const
  _JUDGE_HIGHER_FLAG  # const
  class MismatchResult  # Outcome of comparing structural (S) and judge (J) confidences.
  def detect_mismatch()  # Return whether ``|S - J|`` exceeds ``threshold`` and a trust flag.

**`backend/app/confidence/structural.py`**
  def calculate_coverage()  # C_coverage: covered claims / total sub-claims, authority-weighted (BRD-23 §4.7).
  def calculate_agreement()  # C_agreement: confidence-weighted ratio of non-contradicting evidence.
  def calculate_diversity()  # C_diversity: unique-domain count mapped through a fixed table,
  def apply_echo_chamber_penalty()  # Apply echo-chamber penalty when sources cluster temporally (IP-25 Phase 0).
  def calculate_no_conflict()  # C_no_conflict: 1 - contradictions / evidence, clamped to [0.0, 1.0].
  def calculate_structural_confidence()  # Compose the five S components into a ``StructuralConfidence``.

### `config.py/`

**`backend/app/config.py`**
  class Settings  # Application settings loaded from environment.

### `dependencies.py/`

**`backend/app/dependencies.py`**
  async def get_db()  # Yield a database session for the request lifecycle.
  async def get_current_username()  # Verify caller identity from `X-Username` + `X-Token` headers.

### `domain/`

**`backend/app/domain/confidence.py`**
  class StructuralConfidence  # Structural confidence components.
  class ConfidenceResult  # Full confidence calculation result.

**`backend/app/domain/enums.py`**
  class StopReason  # Terminal states for a run (RF-01, WP-3 amendment 2026-05-27).
  class QuestionType  # Supported question types (RF-06).
  class AnswerKind  # Shape of the answer produced at terminal ``judge_confirmed`` (RF-17).
  class OutputFormat  # Answer format options (RF-10).
  class ComplexityHint  # Question complexity classification for planning budget (BRD-22).
  class EventType  # All event types (45) for the event log.
  class EvidencePolarity  # Polarity of evidence toward a claim.
  class SourceType  # Source plugin identifiers.
  class TemporalSensitivity  # Temporal-sensitivity bucket per BRD-23 §4.4 (WP-1).
  class QuestionDomain  # Topical domain emitted by the classifier (IP-30).
  class AuthorityTier  # Authority-tier bucket per BRD-23 §4.4 / §4.7 (WP-3).
  class Lane  # Research lane selection (IP-25 Phase A).

**`backend/app/domain/events.py`**
  class BaseEvent  # Base class for all events.
  class QuestionAskedEvent  # Initial question submitted by user.
  class QuestionNormalizedEvent  # Grammar/typo normalization of the user's question.
  class QuestionClassifiedEvent  # Question classified with type and complexity hint (BRD-22).
  class SubClaim  # A sub-claim in the research plan.
  class PlanCreatedEvent  # Initial plan with sub-claims decomposition.
  class PlanCritiquedEvent  # Plan critic evaluation (RF-14).
  class PlanRevisedEvent  # Plan updated after critique.
  class HypothesesGeneratedEvent  # Abductive hypotheses generated for causal/scenario questions (IP-25 Phase D).
  class ToolCalledEvent  # Search tool invocation.
  class SourceResult  # A single result from a source.
  class EvidenceAddedEvent  # Evidence collected from a source.
  class ClaimCoveredEvent  # Sub-claim has sufficient evidence.
  class ClaimUncoverableEvent  # Sub-claim cannot be answered with available sources.
  class SourceFailedEvent  # Source plugin returned an error.
  class DeepFetchPerformedEvent  # Full-content fetch performed for a shallow citation (BRD-23 WP-2).
  class QueryReformulatedEvent  # Query reformulation triggered by low relevance scores (IP-25 Phase 0).
  class EchoChamberDetectedEvent  # Echo chamber penalty applied (IP-25 Phase 0).
  class RouteSelectedEvent  # Lane routing decision (IP-25 Phase A).
  class PlanGapsDetectedEvent  # Dynamic re-decomposition triggered (IP-25 Phase B).
  class NoProgressDetectedEvent  # Confidence plateau detected (IP-25 Phase B).
  class LaneEscalatedEvent  # Lane escalation (IP-25 Phase C).
  class AgentThoughtEvent  # Agent reasoning thought in ReAct loop (IP-25 Phase E).
  class AgentActionEvent  # Agent action selection in ReAct loop (IP-25 Phase E).
  class AgentObservationEvent  # Agent observation result in ReAct loop (IP-25 Phase E).
  class HypothesisEvaluatedEvent  # Hypothesis verdict updated (IP-25 Phase E).
  class HistorySummarizedEvent  # ReAct history summarized to prevent token overflow (IP-25 Phase E).
  class VerificationQuestionsGeneratedEvent  # Verification questions generated for CoVe (IP-25 Phase F).
  class CoveContradictionDetectedEvent  # CoVe detected a contradiction in the draft answer (IP-25 Phase F).
  class AmbiguityDetectedEvent  # Question ambiguity detected (RF-04).
  class ContradictionSource  # A source involved in a contradiction.
  class ContradictionDetectedEvent  # Irreconcilable source conflict (RF-04).
  class ContradictionResolvedEvent  # Contradiction resolved through additional evidence.
  class UserContextChallengedEvent  # User context contradicts evidence (RF-07).
  class PriorRunHintReplayedEvent  # Instant-answer cache replay (BRD-22).
  class DraftSynthesizedEvent  # Final draft emitted by the synthesizer (PR-3 Mejora 3.2, RF-03).
  class MetaStopVerdictEvent  # Meta-judge "Value of Continuation" verdict (BRD-26 §4.2, §4.4).
  class AdversarialObjectionsGeneratedEvent  # Three-objection adversarial completeness pass (BRD-26 §4.2 / §4.5).
  class DirectedSubclaimsFromObjectionsEvent  # New sub-claims minted from unanswered objections (BRD-26 §4.10).
  class JudgeRuledEvent  # Judge LLM evaluation (RF-12, WP-3 G5 C_kind_appropriateness, WP-5 extensions).
  class ConfidenceMismatchEvent  # S and J diverge significantly (RF-15).
  class SaturationDetectedEvent  # Novelty-based saturation signal fired (WP-4).
  class JudgeProviderDegradedEvent  # Judge provider failed, fell back to alternate provider (WP-5).
  class AgentErroredEvent  # Unrecoverable error during execution.
  class ResumedAfterErrorEvent  # Run resumed after an error.
  class ResumedAfterCancelEvent  # Run resumed after user cancellation.
  class StopRationale  # Structured 'why we stopped' payload (RF-13 / RF-19, WP-3 G2).
  class AnswerSection  # Section of a structured answer.
  class Citation  # Citation reference in the answer.
  class StoppedEvent  # Terminal event with final answer or budget/error stop.
  class CostIncurredEvent  # One LLM round or Source call cost (BRD-29 §4.4).

**`backend/app/domain/hypothesis.py`**
  class Hypothesis  # A candidate hypothesis for abductive reasoning.

**`backend/app/domain/meta_stop.py`**
  class ValueOfContinuationVerdict  # LLM-produced answer to: 'Is another research round worth it?'
  class Objection  # A single skeptical objection produced by the adversarial reviewer.
  class AdversarialCompletenessVerdict  # Result of the adversarial completeness pass.

**`backend/app/domain/run.py`**
  LLM_PROVIDERS  # const
  class RunCreate  # Request body for creating a new run.
  class RunResponse  # Response model for a run.
  class RunListItem  # Lightweight run for list views (RF-09).
  class RunListPage  # Keyset-paginated page of run summaries (BRD-20 §4.4, RF-09).
  class RunForkRequest  # Request to fork a run from a specific event (RF-03).

**`backend/app/domain/structured.py`**
  class KeyValueRow
  class ParagraphBlock  # A plain prose paragraph rendered as styled text.
  class KeyValueBlock  # Key/value table — for facts, attributes, specs.
  class StepsBlock  # Ordered list of steps / process stages.
  class KeyPointsBlock  # Unordered list of key points / bullets.
  class MermaidBlock  # Mermaid diagram source (flowchart, sequence, etc.).
  class MarkdownBlock  # Fallback for content that is already richly formatted by the LLM.
  class StructuredAnswerData  # Structured-format answer payload (RF-10).

### `exceptions.py/`

**`backend/app/exceptions.py`**
  class LLMContractError  # LLM output violated the expected contract after retries (WP-2).
  class RunNotFoundError  # Run does not exist.
  class RunForbiddenError  # Authenticated caller does not own the target run (BRD-20 AC-05).
  class RunNotFinishedError  # Run is still in progress; cannot delete (BRD-20 AC-04).
  class InvalidCursorError  # Pagination cursor is malformed or tampered with (BRD-20 AC-11).
  class EventNotFoundError  # Event does not exist.
  class RunNotForkableError  # Run cannot be forked from this event (RF-03).
  class RunAlreadyStoppedError  # Run has already stopped and the requested transition is invalid.
  class RunStillRunningError  # Run is still running (cannot resume).
  class UnauthorizedError  # User not authorized for this operation.
  class RunAlreadyRunningError  # A task for this run is already registered (RF-05 single-writer).
  class RunStillTerminatingError  # Prior task did not settle within the resume grace window (BRD-19 §4.6.1).

### `health/`

**`backend/app/health/models.py`**
  class ServiceStatus  # The five terminal states for a single service probe.
  class ServiceCategory  # Functional grouping for the bar UI (BRD-27 §4.7).
  class ServiceHealth  # Single service entry in the health bar.
  class HealthSnapshot  # Response payload for ``GET /api/health/services``.

**`backend/app/health/probes.py`**
  class _ProbeError  # Base class for probe-local exceptions.
  class NoKeyError  # Required environment variable is missing.
  class AuthError  # Upstream rejected the credentials (HTTP 401/403).
  class RateLimitError  # Upstream returned HTTP 429.
  class UnreachableError  # Network-level failure: DNS, connection refused, TLS error.
  class UpstreamError  # Upstream returned a non-success HTTP code (typically 5xx).
  class DisabledError  # Service is intentionally not enabled in this V1 build.
  class ProbeSpec  # Static description of one probe.

**`backend/app/health/registry.py`**
  class HealthRegistry  # Process-local per-probe cache of :class:`ServiceHealth` results.
  def get_registry()
  def reset_registry()  # Clear the cached registry (test-only).

### `llm/`

**`backend/app/llm/classifier_cache.py`**
  _HITS  # const
  _MISSES  # const
  def get()  # Return cached response or ``None``. Pure read; updates LRU recency.
  def put()  # Store ``value`` under the request key. Evicts oldest if over capacity.
  def clear()  # Drop all entries. Used by tests via the autouse fixture.
  def stats()

**`backend/app/llm/client.py`**
  T  # const
  def resolve_active_provider()  # Return the provider name in effect for the current async context.
  def active_judge_model()  # Return the model name that will actually serve the next JUDGE call.
  _TOKEN_ROTATION  # const
  class LLMPoolExhausted  # All GitHub PATs in the rotation pool returned 429 in a single
  class LLMProviderQuotaExhausted  # User-selected non-github provider returned a permanent quota error.
  R  # const
  class LLMClient  # Thin client exposing the single :meth:`call` entry point.
  def count_tokens()  # Count tokens in ``text`` for the given model.

**`backend/app/llm/embeddings.py`**
  async def embed()  # Embed multiple texts using the configured embedding model.

**`backend/app/llm/factory.py`**
  def get_provider()  # Return a singleton instance of the requested non-github provider.
  def reset_provider_cache()  # Drop cached providers — used by tests that flip ``llm_provider``.
  def available_providers()  # Return descriptors of every supported provider for the public API.

**`backend/app/llm/last_error.py`**
  class ProviderError
  def record()  # Record (or overwrite) the most recent error for ``provider``.
  def clear()  # Forget any recorded error for ``provider`` (call after a success).
  def get_recent()  # Return the recorded error if it occurred within ``within_s`` seconds.
  def reset_all()  # Test helper: drop every recorded error.

**`backend/app/llm/meta_judge.py`**
  class MetaJudgeContext  # Minimal projection of run state the meta-judge needs.
  async def evaluate_value_of_continuation()  # Run the Value-of-Continuation pass.
  async def generate_adversarial_objections()  # Run the Adversarial Completeness pass (exactly 3 objections).

**`backend/app/llm/models.py`**
  class QuestionClassification  # Output of the classifier (RF-06 question typing).
  class QuestionNormalization  # Output of the pre-classifier normalizer.
  class SubClaimOutput  # A single sub-claim emitted by the planner.
  class PlanOutput  # Structured plan emitted by the planner.
  class ScenarioBranch  # A scenario branch for predictive/future questions.
  class WeightedCandidate  # A weighted candidate for comparative questions with disagreement.
  class TradeoffCriterion  # A tradeoff criterion for subjective opinion questions.
  class SynthesizedAnswer  # Final answer produced by the synthesizer.
  class JudgeVerdict  # Verdict from the cross-family judge (RF-12, RF-15, WP-5 extensions).
  class MiniJudgeVerdict  # Mini-judge verdict for FAST lane (IP-25 Phase C).
  class CritiqueOutput  # Output of the plan critic step (RF-14).

**`backend/app/llm/pricing.py`**
  def compute_cost()  # Return ``(cost_usd, pricing_source)`` for a single completion.

**`backend/app/llm/prompts.py`**
  def language_name()  # Return a plain-English language name for a BCP-47 code.
  CLASSIFIER_SYSTEM_PROMPT  # const
  PLANNER_SYSTEM_PROMPT  # const
  HYPOTHESES_PROMPT  # const
  SYNTHESIZER_SYSTEM_PROMPT  # const
  JUDGE_SYSTEM_PROMPT  # const
  PLAN_GAPS_PROMPT  # const
  FAST_SYNTH_PROMPT  # const
  FAST_MINI_JUDGE_PROMPT  # const
  META_JUDGE_VOC_PROMPT  # const
  META_JUDGE_ADVERSARIAL_PROMPT  # const
  _SHARED_SYSTEM_BLOCK  # const
  _CONTRADICTIONS_DIRECTIVE  # const
  _KIND_BLOCKS  # const
  _MAX_TOKENS_PER_KIND  # const
  def build_synthesizer_prompt()  # Build the system prompt for the synthesizer based on answer_kind.
  COVE_QUESTIONS_PROMPT  # const
  COVE_VERIFICATION_PROMPT  # const

**`backend/app/llm/providers/_litellm_base.py`**
  T  # const
  class _LiteLLMProvider  # Base for vendors reachable through litellm's native routing.

**`backend/app/llm/providers/anthropic.py`**
  class AnthropicProvider

**`backend/app/llm/providers/base.py`**
  T  # const
  class LLMProvider  # Vendor-agnostic structured-output contract for a single LLM call.

**`backend/app/llm/providers/google.py`**
  class GoogleProvider

**`backend/app/llm/providers/openai.py`**
  class OpenAIProvider

**`backend/app/llm/retry.py`**
  def create_retry_decorator()  # Build a tenacity retry decorator with exponential backoff.

**`backend/app/llm/roles.py`**
  class LLMRole  # The four LLM roles in the Novum research agent (RF-06, RF-12, RF-15).
  class RoleConfig  # Resolved configuration for an :class:`LLMRole`.

### `main.py/`

**`backend/app/main.py`**
  async def lifespan()  # Application lifespan: startup and shutdown.

### `models/`

**`backend/app/models/base.py`**
  class Base  # Base class for all ORM models.

**`backend/app/models/event.py`**
  class Event  # Append-only event in the run event log (RF-03).

**`backend/app/models/run.py`**
  class Run  # Research run with question, configuration, and terminal state.

**`backend/app/models/user.py`**
  class User  # User model with username-only identity (RF-05).

### `output/`

**`backend/app/output/prose.py`**
  class ProseRenderer  # Renders the answer as flowing narrative text with source citations.

**`backend/app/output/registry.py`**
  class RendererRegistry

**`backend/app/output/structured.py`**
  class StructuredRenderer  # Renders the answer with tables, diagrams, and organised sections.

### `routes/`

**`backend/app/routes/auth.py`**
  class RegisterRequest  # Body for `POST /api/auth/register`.
  class RegisterResponse  # Response containing the newly created identity.
  class VerifyRequest  # Body for `POST /api/auth/verify`.
  class VerifyResponse  # Result of a verification request.
  class UserResponse  # Public-profile payload.
  async def register()  # Create or re-token a user identity. Token is returned exactly once.
  async def verify()  # Non-throwing verify — answers `{valid: false}` on any failure.
  async def get_user()  # Public profile lookup.

**`backend/app/routes/cost_analytics.py`**
  class AnalyticsTotals
  class ProviderBreakdown
  class KindBreakdown
  class UserBreakdown
  class ModelBreakdown
  class DailyPoint
  class CostRow
  class CostAnalyticsResponse
  async def get_cost_analytics()  # Aggregate cost events across all runs (global, not owner-scoped).

**`backend/app/routes/costs.py`**
  class ProviderCostRow
  class RunCostsResponse
  async def get_run_costs()  # Return the per-provider/kind/model/task cost breakdown for a run.

**`backend/app/routes/events.py`**
  async def stream_events()  # Stream events for a run via SSE (RF-08).

**`backend/app/routes/formats.py`**
  async def list_formats()  # List available output formats (RF-10).

**`backend/app/routes/health.py`**
  async def health()  # Liveness probe — returns 200 with ``{"status": "ok"}``.
  async def service_health()  # Aggregated per-service health snapshot (cached 30 s server-side).

**`backend/app/routes/llm.py`**
  class ProviderInfo
  class ProviderListResponse
  async def list_providers()  # Return all supported providers with their availability.

**`backend/app/routes/runs.py`**
  async def create_run()  # Create a new research run (RF-01).
  async def list_runs()  # Keyset list of runs across all users (RF-05 public history).
  async def get_run()  # Get a run by ID — public-by-URL per RF-05.
  async def delete_run()  # Delete a finished run — public-by-URL per RF-05.
  async def fork_run()  # Fork a run from a specific event (RF-03).
  async def cancel_run()  # Cancel a running run (RF-08).
  async def resume_run()  # Resume a stopped/errored run (RF-11).

### `seams/`

**`backend/app/seams/output.py`**
  class RenderContext  # Context passed to a renderer at answer-stop time.
  class RenderedOutput  # Output produced by a renderer.
  class OutputRenderer  # Protocol for output renderer plugins (RF-10).

**`backend/app/seams/source.py`**
  class SourceResult  # A single result returned by a Source.search call.
  class SourceError  # Raised when a source search or health check fails.
  class Source  # Protocol for source plugins.

**`backend/app/seams/stopping.py`**
  class SignalResult  # Outcome categories for a single stopping signal evaluation.
  class StopSignalOutput  # Result returned by ``StoppingSignal.evaluate``.
  class StopContext  # Immutable snapshot of run state passed to every signal.
  class StoppingSignal  # Protocol for stopping-signal plugins.

### `services/`

**`backend/app/services/auth_service.py`**
  class InvalidTokenError  # Raised when verification fails (unknown user OR wrong token).
  class AuthService  # User identity operations backed by `AsyncSession`.

**`backend/app/services/event_service.py`**
  class EventService  # Service for event operations (BRD-03 §4.6).

**`backend/app/services/run_service.py`**
  class RunService  # Service for run operations (BRD-03 §4.5).

### `sources/`

**`backend/app/sources/_cost.py`**
  async def emit_source_cost()  # Emit one ``CostIncurredEvent`` for a Source call.

**`backend/app/sources/base.py`**
  DEFAULT_MAX_CONTENT_CHARS  # const
  class BaseSource  # Concrete mixin with shared helpers.

**`backend/app/sources/openalex.py`**
  _BASE_URL  # const
  _SELECT_FIELDS  # const
  _DEFAULT_QUALITY_FILTERS  # const
  _DEFAULT_SORT  # const
  class OpenAlexSource  # OpenAlex Works API client.

**`backend/app/sources/pricing.py`**
  def tavily_cost()  # Tavily: 1 credit for ``basic``, 2 for ``advanced``.
  def wikipedia_cost()
  def free_source_cost()

**`backend/app/sources/registry.py`**
  class SourceRegistry  # Registry of available source plugins.
  def get_registry()
  def get_source()
  def reset_registry()  # Clear the cached registry (test-only).

**`backend/app/sources/semantic_scholar.py`**
  _BASE_URL  # const
  _SEARCH_FIELDS  # const
  class SemanticScholarSource  # Semantic Scholar Graph API client.

**`backend/app/sources/tavily.py`**
  class TavilySource  # Tavily web search implementation.

**`backend/app/sources/wikipedia.py`**
  _USER_AGENT  # const
  class WikipediaSource  # Wikipedia search and content extraction via the MediaWiki API.

### `sse/`

**`backend/app/sse/manager.py`**
  _QUEUE_MAXSIZE  # const
  class ConnectionManager  # Tracks active SSE connections, cancellation flags and subscribers per run.

**`backend/app/sse/stream.py`**
  class _CancellationProbe  # Minimal surface required from ``connection_manager`` by the stream.
  class _EventReader  # Minimal surface required from ``EventService`` by the stream.
  async def event_stream()  # Yield SSE frames for ``run_id`` until termination.

### `stopping/`

**`backend/app/stopping/policy.py`**
  class StoppingPolicy  # Coordinates layered stopping signals (E + F + A + D + B).

**`backend/app/stopping/react_intra_loop.py`**
  class HypothesisConfirmedSignal  # Fires when ≥1 hypothesis confirmed AND structural confidence ≥ 0.75.
  class AllHypothesesRefutedSignal  # Fires when all hypotheses are refuted → best effort answer.
  class ReactStepCapSignal  # Fires when react_step_count >= max_react_steps.
  class ReactContradictionSignal  # Fires when ≥2 confirmed hypotheses with disjoint primary_authoritative evidence.
  async def evaluate_react_intra_loop()  # Aggregate helper to evaluate all ReAct intra-loop signals.

**`backend/app/stopping/signals/agreement.py`**
  class AgreementSignal  # Layer D — agreement gate.

**`backend/app/stopping/signals/budget.py`**
  class BudgetSignal  # Layer F — search-round budget safety net.

**`backend/app/stopping/signals/coverage.py`**
  class CoverageSignal  # Layer A — coverage gate.

**`backend/app/stopping/signals/honest.py`**
  class HonestStopSignal  # Layer E — DEPRECATED (WP-3, always returns DEFER).

**`backend/app/stopping/signals/judge.py`**
  class JudgeSignal  # Layer B — judge confirmation gate (lowest priority).

**`backend/app/stopping/signals/no_progress.py`**
  class NoProgressSignal  # Layer C — confidence plateau checkpoint.
  async def check_no_progress()  # Check if confidence has plateaued over the last 3 rounds.
  def check_event_level_plateau()  # Fire when the last ``window`` emitted events contain no progress markers.
  def check_claim_coverage_plateau()  # Fire when the last ``rounds`` analyze cycles added 0 new covered claims.

**`backend/app/stopping/signals/saturation.py`**
  async def evaluate_saturation()  # Evaluate if evidence collection has saturated (novelty < threshold).

**`backend/app/stopping/signals/stuck_planner.py`**
  def check_stuck_planner()  # Fire when the last two reformulation generations share >= ``min_overlap`` URLs.

---

## Frontend (`frontend/src/`)

### `App.tsx/`

**`frontend/src/App.tsx`**
  function App()

### `components/`

**`frontend/src/components/atoms/AnimatedDots.tsx`**
  interface AnimatedDotsProps
  function AnimatedDots()

**`frontend/src/components/atoms/BackgroundOrbs.tsx`**
  function BackgroundOrbs()

**`frontend/src/components/atoms/Badge.tsx`**
  type BadgeVariant
  interface BadgeProps
  function Badge()

**`frontend/src/components/atoms/BlinkingCursor.tsx`**
  interface BlinkingCursorProps
  function BlinkingCursor()

**`frontend/src/components/atoms/Button.tsx`**
  type ButtonVariant
  type ButtonSize
  interface ButtonProps
  const Button

**`frontend/src/components/atoms/CollapseToggleButton.tsx`**
  interface CollapseToggleButtonProps
  function CollapseToggleButton()

**`frontend/src/components/atoms/EventIcon.tsx`**
  interface EventIconProps
  function EventIcon()

**`frontend/src/components/atoms/EventPayloadViewer.tsx`**
  interface EventPayloadViewerProps
  function EventPayloadViewer()

**`frontend/src/components/atoms/FeedRail.tsx`**
  interface FeedRailProps
  function FeedRail()

**`frontend/src/components/atoms/FeedStepIcon.tsx`**
  interface FeedStepIconProps
  function FeedStepIcon()

**`frontend/src/components/atoms/ForkableEventRow.tsx`**
  interface ForkableEventRowProps
  function ForkableEventRow()

**`frontend/src/components/atoms/GlassSurface.tsx`**
  type GlassVariant
  type GlassElevation
  type GlassRadius
  interface GlassSurfaceProps
  const GlassSurface

**`frontend/src/components/atoms/JumpToLatestPill.tsx`**
  interface JumpToLatestPillProps
  function JumpToLatestPill()

**`frontend/src/components/atoms/KpiCard.tsx`**
  interface KpiCardProps
  function KpiCard()

**`frontend/src/components/atoms/Logo.tsx`**
  interface LogoProps
  function Logo()

**`frontend/src/components/atoms/OutcomeBar.tsx`**
  type OutcomeVariant
  interface OutcomeBarProps
  function OutcomeBar()

**`frontend/src/components/atoms/ProviderIcon.tsx`**
  interface ProviderIconProps
  function ProviderIcon()

**`frontend/src/components/atoms/ProviderSelect.tsx`**
  interface ProviderSelectProps
  function ProviderSelect()

**`frontend/src/components/atoms/ServiceStatusDot.tsx`**
  interface ServiceStatusDotProps
  function ServiceStatusDot()

**`frontend/src/components/atoms/SourceLinkRow.tsx`**
  interface SourceLinkRowProps
  function SourceLinkRow()

**`frontend/src/components/atoms/Spinner.tsx`**
  type SpinnerSize
  interface SpinnerProps
  function Spinner()

**`frontend/src/components/atoms/ThemeToggleIcon.tsx`**
  interface ThemeToggleIconProps
  function ThemeToggleIcon()

**`frontend/src/components/atoms/ThinkingDots.tsx`**
  interface ThinkingDotsProps
  function ThinkingDots()

**`frontend/src/components/molecules/AnalyticsFilters.tsx`**
  interface AnalyticsFiltersProps
  function AnalyticsFilters()

**`frontend/src/components/molecules/AnswerToolbar.tsx`**
  type AnswerViewMode
  interface AnswerToolbarProps
  function AnswerToolbar()

**`frontend/src/components/molecules/AuthorityTierChip.tsx`**
  interface AuthorityTierChipProps
  function AuthorityTierChip()

**`frontend/src/components/molecules/ChartFrame.tsx`**
  interface ChartFrameProps
  function ChartFrame()

**`frontend/src/components/molecules/ComplexityBadge.tsx`**
  interface ComplexityBadgeProps
  function ComplexityBadge()

**`frontend/src/components/molecules/ConfidenceBar.tsx`**
  interface ConfidenceBarProps
  function ConfidenceBar()

**`frontend/src/components/molecules/CostDonut.tsx`**
  interface CostDonutProps
  function CostDonut()

**`frontend/src/components/molecules/CostLineChart.tsx`**
  interface CostLineChartProps
  function CostLineChart()

**`frontend/src/components/molecules/DeepFetchEntry.tsx`**
  interface DeepFetchEntryProps
  function DeepFetchEntry()

**`frontend/src/components/molecules/ElapsedClock.tsx`**
  interface ElapsedClockProps
  function formatElapsed()
  function ElapsedClock()

**`frontend/src/components/molecules/EventNode.tsx`**
  interface TraceEventInput
  interface EventNodeProps
  function EventNode()

**`frontend/src/components/molecules/ExpectedExpertsList.tsx`**
  interface ExpectedExpertsListProps
  function ExpectedExpertsList()

**`frontend/src/components/molecules/FeedStep.tsx`**
  interface FeedStepProps
  function FeedStep()

**`frontend/src/components/molecules/FeedStepLine.tsx`**
  interface FeedStepLineProps
  function FeedStepLine()

**`frontend/src/components/molecules/FormatSelector.tsx`**
  interface FormatSelectorProps
  function FormatSelector()

**`frontend/src/components/molecules/IdentitySlot.tsx`**
  function IdentitySlot()

**`frontend/src/components/molecules/JudgeVerdictCard.tsx`**
  interface JudgeVerdictCardProps
  function JudgeVerdictCard()

**`frontend/src/components/molecules/KindBarChart.tsx`**
  interface KindBarChartProps
  function KindBarChart()

**`frontend/src/components/molecules/LineageBadge.tsx`**
  interface LineageBadgeProps
  function LineageBadge()

**`frontend/src/components/molecules/MetaRow.tsx`**
  interface MetaRowProps
  function MetaRow()

**`frontend/src/components/molecules/PlanPreview.tsx`**
  interface PlanPreviewProps
  const PLAN_PREVIEW_STEPS
  function PlanPreview()

**`frontend/src/components/molecules/PlanStepCard.tsx`**
  interface SubClaim
  interface PlanStepCardProps
  function PlanStepCard()

**`frontend/src/components/molecules/SearchStepCard.tsx`**
  interface SearchSource
  interface SearchStepCardProps
  function SearchStepCard()

**`frontend/src/components/molecules/ServiceHealthWarningModal.tsx`**
  interface ServiceHealthWarningModalProps
  function ServiceHealthWarningModal()

**`frontend/src/components/molecules/ServiceStatusPill.tsx`**
  interface ServiceStatusPillProps
  function ServiceStatusPill()

**`frontend/src/components/molecules/StatusBadge.tsx`**
  type RunStatus
  interface StatusBadgeProps
  function StatusBadge()

**`frontend/src/components/molecules/StatusDot.tsx`**
  interface StatusDotProps
  function StatusDot()

**`frontend/src/components/molecules/SuggestionChips.tsx`**
  interface SuggestionChipsProps
  const DEFAULT_SUGGESTIONS
  function SuggestionChips()

**`frontend/src/components/molecules/TemporalSensitivityBadge.tsx`**
  interface TemporalSensitivityBadgeProps
  function TemporalSensitivityBadge()

**`frontend/src/components/molecules/ThemeToggle.tsx`**
  function ThemeToggle()

**`frontend/src/components/molecules/Toaster.tsx`**
  function Toaster()

**`frontend/src/components/molecules/TopModelsChart.tsx`**
  interface TopModelsChartProps
  function TopModelsChart()

**`frontend/src/components/molecules/TraceEmpty.tsx`**
  interface TraceEmptyProps
  const TRACE_EMPTY_MESSAGE
  function TraceEmpty()

**`frontend/src/components/molecules/UserBarChart.tsx`**
  interface UserBarChartProps
  function UserBarChart()

**`frontend/src/components/organisms/ActionBar.tsx`**
  interface ActionBarProps
  function ActionBar()

**`frontend/src/components/organisms/CenterPanelView.tsx`**
  interface CenterPanelViewProps
  function CenterPanelView()

**`frontend/src/components/organisms/CostAnalyticsTable.tsx`**
  interface CostAnalyticsTableProps
  function CostAnalyticsTable()

**`frontend/src/components/organisms/CostDashboard.tsx`**
  interface CostDashboardProps
  function CostDashboard()

**`frontend/src/components/organisms/ForkModal.tsx`**
  interface ForkModalEvent
  interface ForkModalProps
  function ForkModal()

**`frontend/src/components/organisms/HistoryFilters.tsx`**
  interface HistoryFiltersProps
  function hasActiveFilters()
  function HistoryFilters()

**`frontend/src/components/organisms/HistoryItem.tsx`**
  interface HistoryItemProps
  const HistoryItem

**`frontend/src/components/organisms/HistoryList.tsx`**
  interface HistoryListProps
  function HistoryList()

**`frontend/src/components/organisms/NotFoundCard.tsx`**
  interface NotFoundCardProps
  function NotFoundCard()

**`frontend/src/components/organisms/QuestionDisplay.tsx`**
  interface QuestionDisplayProps
  function QuestionDisplay()

**`frontend/src/components/organisms/QuestionForm.tsx`**
  interface QuestionFormValues
  interface QuestionFormProps
  function QuestionForm()
  const QUESTION_FORM_LIMITS

**`frontend/src/components/organisms/RateLimitModal.tsx`**
  interface RateLimitModalProps
  function RateLimitModal()

**`frontend/src/components/organisms/ResearchingBanner.tsx`**
  interface ResearchingBannerLatestEvent
  interface ResearchingBannerProps
  function ResearchingBanner()

**`frontend/src/components/organisms/RunFeed.tsx`**
  interface RunFeedProps
  function RunFeed()

**`frontend/src/components/organisms/RunHeader.tsx`**
  interface RunHeaderProps
  function RunHeader()

**`frontend/src/components/organisms/RunRow.tsx`**
  interface RunRowProps
  const RunRow

**`frontend/src/components/organisms/ServiceStatusBar.tsx`**
  interface ServiceStatusBarProps
  function ServiceStatusBar()

**`frontend/src/components/organisms/SourcesCard.tsx`**
  interface SourceEntry
  interface SourcesCardProps
  function SourcesCard()

**`frontend/src/components/organisms/StopReasonCard.tsx`**
  type StopReasonVariant
  const stopReasonConfig
  interface StopReasonCardProps
  function StopReasonCard()

**`frontend/src/components/organisms/StructuredAnswer.tsx`**
  interface StructuredAnswerProps
  function StructuredAnswer()

**`frontend/src/components/organisms/StructuredBlocks.tsx`**
  interface StructuredBlocksProps
  function StructuredBlocks()

**`frontend/src/components/organisms/TraceHeader.tsx`**
  interface TraceHeaderProps
  function TraceHeader()

**`frontend/src/components/organisms/TraceTimeline.tsx`**
  interface TraceTimelineEvent
  interface TraceTimelineProps
  function TraceTimeline()

**`frontend/src/components/organisms/TrustSummary.tsx`**
  interface JudgeConfidenceMetrics
  interface StructuralConfidenceFallback
  interface StopRationaleInfo
  interface TrustSummaryProps
  function TrustSummary()

**`frontend/src/components/organisms/UsernameModal.tsx`**
  interface UsernameModalProps
  function UsernameModal()

**`frontend/src/components/organisms/UsernameModalContainer.tsx`**
  function UsernameModalContainer()

**`frontend/src/components/templates/AppShell.tsx`**
  type Breakpoint
  interface AppShellProps
  function AppShell()

**`frontend/src/components/templates/CenterPanel.tsx`**
  interface CenterPanelProps
  function CenterPanel()

**`frontend/src/components/templates/HistoryPanel.tsx`**
  interface HistoryPanelProps
  function HistoryPanel()

**`frontend/src/components/templates/TracePanel.tsx`**
  interface TracePanelProps
  function TracePanel()

### `hooks/`

**`frontend/src/hooks/useCostAnalytics.ts`**
  const costAnalyticsQueryKey
  function useCostAnalytics()

**`frontend/src/hooks/useCreateRun.ts`**
  interface UseCreateRunResult
  function useCreateRun()

**`frontend/src/hooks/useLoginModal.ts`**
  const useLoginModal

**`frontend/src/hooks/usePathname.ts`**
  function usePathname()

**`frontend/src/hooks/useRun.ts`**
  interface UseRunResult
  const runQueryKey
  interface UseRunOptions
  function useRun()

**`frontend/src/hooks/useRunHistory.ts`**
  function mapRun()
  interface UseRunHistoryOptions
  function useRunHistory()
  function useDeleteRun()

**`frontend/src/hooks/useRunStream.ts`**
  interface UseRunStreamOptions
  interface UseRunStreamResult
  function useRunStream()

**`frontend/src/hooks/useServiceHealth.ts`**
  const SERVICE_HEALTH_QUERY_KEY
  function useServiceHealth()

**`frontend/src/hooks/useTheme.ts`**
  interface UseThemeReturn
  function useTheme()

**`frontend/src/hooks/useToast.ts`**
  interface ToastApi
  function useToast()

### `lib/`

**`frontend/src/lib/answerAnimation.ts`**
  function hasAnswerBeenAnimated()
  function markAnswerAnimated()
  function isAnimateAnswerEnabled()
  function setAnimateAnswerEnabled()

**`frontend/src/lib/api/costAnalytics.ts`**
  async function fetchCostAnalytics()

**`frontend/src/lib/api.ts`**
  interface ApiError
  class ApiClientError
  const api
  interface RunListItemDto
  interface ListRunsParams
  interface RunListPageDto
  async function listRuns()
  async function deleteRun()
  type StopReasonDto
  type QuestionTypeDto
  type OutputFormatDto
  interface RunResponseDto
  async function getRun()
  async function cancelRun()
  async function resumeRun()
  interface RunCreatePayload
  async function createRun()
  async function forkRun()
  async function getServiceHealth()

**`frontend/src/lib/auth.ts`**
  interface UserIdentity
  function getStoredIdentity()
  function storeIdentity()
  function clearIdentity()
  function getAuthHeaders()

**`frontend/src/lib/clipboard.ts`**
  async function copyToClipboard()
  async function copyRunUrl()
  async function copyDiffUrl()

**`frontend/src/lib/cn.ts`**
  function cn()

**`frontend/src/lib/constants.ts`**
  const API_URL
  const DEMO_SLOWDOWN

**`frontend/src/lib/costAnalyticsFormat.ts`**
  const CHART_COLORS
  const PROVIDER_COLORS
  const KIND_COLORS
  function colorForProvider()
  function colorForKind()
  function formatUsd()
  function formatTokens()
  function formatInt()
  function formatShortDate()
  function todayIso()
  function daysAgoIso()

**`frontend/src/lib/eventLabels.ts`**
  const COMPLEXITY_LABELS
  const QUESTION_TYPE_LABELS
  const QUESTION_DOMAIN_LABELS
  const EVENT_LABELS
  const EVENT_ACTIVITIES
  function getEventLabel()
  function getEventActivity()
  function getEventNarrative()

**`frontend/src/lib/eventVisuals.ts`**
  type EventTone
  interface EventVisual
  const EVENT_VISUALS
  function getEventVisual()
  const TONE_COLOR
  const EXPANDED_BY_DEFAULT
  const DECISION_EVENTS
  function isDecisionEvent()

**`frontend/src/lib/feedGrouping.ts`**
  interface FeedStepData
  function buildFeedSteps()

**`frontend/src/lib/format.ts`**
  function formatRelative()
  function formatISO()
  function truncate()
  function formatElapsed()

**`frontend/src/lib/idleMessages.ts`**
  const IDLE_REASSURANCE_MESSAGES
  function useIdleReassurance()

**`frontend/src/lib/microcopy.ts`**
  const POST_RESUME_NOTICE
  const FORK_MODAL_EMPTY_STATE
  const FORK_MODAL_TITLE
  const FORK_MODAL_DESCRIPTION
  const LINEAGE_BADGE_LABEL
  const RATE_LIMIT_MODAL_TITLE
  const RATE_LIMIT_MODAL_DESCRIPTION
  const RATE_LIMIT_MODAL_HINT
  const RATE_LIMIT_MODAL_CLOSE
  const FEED_LET_ME_SEARCH
  const FEED_LET_ME_FETCH
  const FEED_LET_ME_THINK
  const FEED_SEARCHED_WEB
  const FEED_FETCHED_PAGE
  const FEED_DONE
  const FEED_TOGGLE_COLLAPSE
  const FEED_TOGGLE_EXPAND
  const TRACE_PANEL_COLLAPSE
  const TRACE_PANEL_EXPAND
  const ANSWER_SKIP_HINT
  const ANSWER_ANIMATE_TOGGLE
  const ANSWER_KIND_BEST_EFFORT_LABEL
  const ANSWER_KIND_BEST_EFFORT_DESCRIPTION
  function FEED_RESULTS_COUNT()
  function FEED_REASONING_TRACE()

**`frontend/src/lib/motion.ts`**
  const fadeUp
  const stagger

**`frontend/src/lib/providers.ts`**
  const LLM_PROVIDERS
  type LlmProviderName
  const PROVIDER_LABELS
  const DEFAULT_PROVIDER
  interface ProviderInfo
  interface ProviderListResponse
  function getStoredProvider()
  function setStoredProvider()
  async function fetchProviders()

**`frontend/src/lib/queryClient.ts`**
  const queryClient

**`frontend/src/lib/sse.ts`**
  interface SSEOptions
  interface SSEConnection
  function createSSEConnection()
  function parseSSEEvent()
  function addNamedListener()

**`frontend/src/lib/structuredAnswerMarkdown.ts`**
  function structuredAnswerToMarkdown()

**`frontend/src/lib/theme.ts`**
  type Theme
  const THEME_STORAGE_KEY
  const DEFAULT_THEME
  const SYSTEM_LIGHT_MEDIA_QUERY
  function isTheme()
  function getStoredTheme()
  function getSystemTheme()
  function readStoredTheme()
  function writeStoredTheme()
  function applyThemeToDocument()

**`frontend/src/lib/useTypewriter.ts`**
  interface UseTypewriterOptions
  interface UseTypewriterResult
  function useTypewriter()

### `main.tsx/`

**`frontend/src/main.tsx`**
  function __resetAppBootForTests()
  interface AppBootProps
  function AppBoot()

### `pages/`

**`frontend/src/pages/CenterPanelContainer.tsx`**
  function CenterPanelContainer()

**`frontend/src/pages/CostAnalyticsPage.tsx`**
  function CostAnalyticsPage()

**`frontend/src/pages/DiffPage.tsx`**
  function DiffPage()

**`frontend/src/pages/HistoryPanelContainer.tsx`**
  function HistoryPanelContainer()

**`frontend/src/pages/HomePage.tsx`**
  function HomePage()

**`frontend/src/pages/HowWeWorkPage.tsx`**
  function HowWeWorkPage()

**`frontend/src/pages/NewRunContainer.tsx`**
  function NewRunContainer()

**`frontend/src/pages/RunPage.tsx`**
  function RunPage()

**`frontend/src/pages/TracePanelContainer.tsx`**
  function TracePanelContainer()

### `router.tsx/`

**`frontend/src/router.tsx`**
  function ProtectedRoute()
  const router

### `stores/`

**`frontend/src/stores/selectionStore.ts`**
  interface SelectionState
  const useSelectionStore

**`frontend/src/stores/toastStore.ts`**
  type ToastKind
  interface Toast
  interface ToastInput
  const useToastStore

**`frontend/src/stores/userStore.ts`**
  const useUserStore

### `types/`

**`frontend/src/types/costAnalytics.ts`**
  interface AnalyticsTotals
  interface ProviderBreakdown
  interface KindBreakdown
  interface UserBreakdown
  interface ModelBreakdown
  interface DailyPoint
  interface CostRow
  interface CostAnalyticsResponse
  interface CostAnalyticsFilters

**`frontend/src/types/events.ts`**
  type StopReason
  type QuestionType
  type QuestionDomain
  type OutputFormat
  type EvidencePolarity
  type SourceType
  type EventType
  type ComplexityHint
  type TemporalSensitivity
  type AuthorityTier
  type AnswerKind
  type Lane
  interface RunStreamEvent
  const FORKABLE_EVENTS
  interface KeyValueRow
  interface ParagraphBlock
  interface KeyValueBlock
  interface StepsBlock
  interface KeyPointsBlock
  interface MermaidBlock
  interface MarkdownBlock
  type StructuredBlock
  interface StructuredAnswerData
  const EventSchema

**`frontend/src/types/health.ts`**
  type ServiceStatus
  type ServiceCategory
  interface ServiceHealth
  interface HealthSnapshot

**`frontend/src/types/history.ts`**
  type RunStatus
  interface RunSummary
  interface HistoryFilterValues
  interface RunHistoryPage

**`frontend/src/types/run.ts`**
  type RunStatus
  interface Run
  function deriveStatus()
  function mapRun()
